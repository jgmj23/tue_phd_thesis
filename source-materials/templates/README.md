# Templates

This folder is for external thesis templates or example projects used as references.

The copied Abraham Cano draft template is kept local-only by `.gitignore`. It is useful for structure, page geometry, and the custom TU/e LaTeX title-page approach, but its thesis prose, figures, and publication PDFs should not become this thesis source.

