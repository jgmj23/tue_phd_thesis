\chapter{Integrated Quantum Sources for Quantum Networks}
\label{ch:quantum-sources}

\section{From Photonic Devices to Network Components}

Quantum-secured networks depend on physical devices whose behavior must remain predictable after they leave a controlled laboratory setting. Building a robust quantum network requires bridging the gap between idealized cryptographic protocols and the imperfect physical reality of the devices that implement them. Entangled-photon sources are one such foundational device class: they determine the spectral structure, brightness, purity, and long-term stability of the quantum states that later protocols consume, ultimately dictating the secure key rate (SKR) and quantum bit error rate (QBER) of the system. If these source properties drift with fabrication tolerances, thermal changes, or uncontrolled mode interactions, then the network layer inherits an unstable security and performance foundation.

The discussion begins at the device level. It examines dual-coupled micro-ring resonators as integrated sources for frequency-bin entangled photons. A central question is not only whether such a device can generate useful quantum states, but whether it can be designed, tuned, and modeled in a way that makes it suitable for larger quantum--classical network systems.

\paragraph{Scope and assumptions.} This dissertation is a systems and networking contribution rather than a study of quantum-mechanical foundations. It assumes familiarity with the basic formalism of quantum states, measurement, entanglement, and the no-cloning theorem, and treats these as building blocks instead of deriving them. Readers seeking that background are referred to standard texts~\cite{nielsen2010quantum}, or to the author's master's thesis~\cite{meyer2024master}. The contributions reported here lie in how these primitives are designed, accelerated, stabilized, and integrated across the device, protocol, and infrastructure layers.

\section{Nonlinear Optics and Four-Wave Mixing}
\label{sec:qsrc-nonlinear}

The generation of entangled photon pairs in integrated photonics relies on nonlinear optical processes. In centrosymmetric materials such as silicon and silicon nitride, the crystal symmetry strictly forbids second-order nonlinearity. Thus, the third-order susceptibility, $\chi^{(3)}$, becomes the lowest-order (and therefore dominant) nonlinear response available. This nonlinearity mediates spontaneous four-wave mixing (SFWM), the process by which two pump photons are annihilated to create a signal and an idler photon pair.

Two conservation laws constrain the process. Energy conservation fixes the
generated frequencies relative to the pump, while momentum conservation (phase
matching) constrains the propagation constants:
\begin{align}
  2\omega_p &= \omega_s + \omega_i, \label{eq:sfwm-energy}\\
  2k_p &= k_s + k_i + \Delta k, \label{eq:sfwm-momentum}
\end{align}
where $\omega_p,\omega_s,\omega_i$ are the pump, signal, and idler frequencies,
$k_p,k_s,k_i$ the corresponding propagation constants, and $\Delta k$ the phase
mismatch. Equation~\eqref{eq:sfwm-energy} places the signal and idler on
frequencies symmetrically detuned about the pump. The condition for efficient
pair generation is that Equation~\eqref{eq:sfwm-momentum} is also satisfied ($\Delta k \to 0$).

The efficiency of SFWM is highly dependent on the spatial overlap of the optical modes within the waveguide and the nonlinear refractive index of the material. Silicon nitride ($\text{Si}_3\text{N}_4$) has emerged as a leading platform for SFWM due to its ultra-low linear propagation loss, wide transparency window spanning from the visible to the mid-infrared, and absence of two-photon absorption at telecom wavelengths. These properties allow for the use of high-power continuous-wave pumps, maximizing the pair generation rate while maintaining high spectral purity.

By carefully engineering the waveguide cross-section, the group velocity dispersion (GVD) can be tailored to navigate a fundamental trade-off: flattening dispersion generates many symmetric mode pairs for multiplexed quantum communication, whereas restricting it yields fewer, highly pure modes with high quality factors ($Q$) essential for quantum repeater networks.

\section{Microring Entanglement Sources}
\label{sec:qsrc-microring}

Integrated photonics addresses the device-level bottleneck in deployable quantum
communication. Traditional tabletop optics, with discrete non-linear crystals and
mirrors, are too fragile, bulky, and expensive for wide-scale deployment. Photonic
integrated circuits (PICs) aim to miniaturize these components onto a semiconductor
chip using fabrication processes compatible with existing CMOS or telecom infrastructure.

For entanglement-based QKD and future repeater networks, the photon source is the critical component. It determines
the spectral structure, brightness, purity, and long-term stability of the photon pairs
that every subsequent protocol stage consumes~\cite{Wehner2018}. If a source emits
multiphoton pulses or indistinguishable spectral modes drift due to temperature fluctuations,
the visibility of quantum interference drops, the QBER rises,
and the protocol may fail to generate secure keys.

The work in this chapter focuses on microring resonators as candidate sources for
frequency-bin entanglement via spontaneous four-wave mixing (SFWM). In a microring,
light circulates and interferes constructively only at specific resonant frequencies
separated by the FSR. For a ring of round-trip length $L$, resonances occur
where the round trip is an integer number of wavelengths, so that the frequency
spacing between adjacent modes is
\begin{equation}
  \Delta\nu_\mathrm{FSR} = \frac{c}{n_g\,L},
  \label{eq:fsr}
\end{equation}
where $c$ is the speed of light and $n_g$ is the group index of the waveguide
mode. Equation~\eqref{eq:fsr} sets the comb spacing available for channelised
operation: a smaller ring widens the FSR, while dispersion (through $n_g$) makes
it weakly frequency-dependent. In this resonant structure, the symmetric signal and idler frequencies coincide with cavity resonances only when Equation~\eqref{eq:sfwm-momentum} is satisfied ($\Delta k \to 0$), which is the condition for efficient, resonantly enhanced pair generation.

Figure~\ref{fig:sfwm-comb} shows the resulting picture: a pump tuned to one resonance of the FSR comb seeds signal--idler pairs on the symmetric resonances around it, with the number of usable bin pairs set by the dispersion-controlled phase-matching envelope.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\linewidth]{figures/chapter3/sfwm_comb.pdf}
  \caption[SFWM frequency-bin generation in a microring comb]{Spontaneous
  four-wave mixing in a microring resonance comb. Two pump photons on the
  central resonance ($\omega_p$, blue) are annihilated to create a signal
  ($\omega_s$, green) and idler ($\omega_i$, orange) photon on resonances
  placed symmetrically about the pump, as required by energy conservation
  (Eq.~\ref{eq:sfwm-energy}). Resonances are
  spaced by the FSR (Eq.~\ref{eq:fsr}). The dashed phase-matching envelopes,
  set by group-velocity dispersion ($\Delta k$), weight how many symmetric bin pairs receive
  appreciable gain and therefore the dimensionality of the frequency-bin
  entangled state.}
  \label{fig:sfwm-comb}
\end{figure}

Key physics and engineering considerations
include:

\begin{itemize}
    \item \textbf{Quality Factor ($Q$):} A measure of the resonator's photon lifetime.
    High-$Q$ rings generate highly coherent, narrow-linewidth photons but are extremely
    sensitive to fabrication variations and thermal drift.
    \item \textbf{Thermal Tuning and Stability:} Refractive indices are temperature-dependent.
    Maintaining precise alignment with standardized telecom grids requires continuous,
    active thermal tuning, which can inadvertently affect the FSR and disrupt entanglement symmetry.
    \item \textbf{Spectral Purity (Schmidt Number):} A mathematically pure bipartite entangled
    state has a Schmidt number of 1. Real sources suffer from spectral correlations
    that degrade purity, necessitating precise dispersion engineering or tight filtering.
    \item \textbf{Platform Tradeoffs:} Silicon nitride ($\text{Si}_3\text{N}_4$) offers ultra-low
    loss and broad transparency but lacks the strong electro-optic (Pockels) effect of
    lithium niobate ($\text{LiNbO}_3$), meaning the choice of material dictates the tuning mechanisms
    available~\cite{Kumar2015,Mao2018,Grebenchukov2023}.
\end{itemize}

To provide the precise control required for networking, the dual-coupled architecture (Figure~\ref{fig:dcm-source}) adds a tunable auxiliary resonator to the main photon-pair generation ring. This additional, tunable degree of freedom can be used to reshape the spectral response of the source, compensate for nonidealities, and expose a control interface between device physics and system-level metrics such as channel spacing, heralded purity, and Schmidt number.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\linewidth]{figures/chapter3/dcm_source.pdf}
  \caption[Dual-coupled microring entanglement source]{Single versus
  dual-coupled microring sources and their transmission spectra.
  \textbf{(a)}~A single main ring side-coupled to a bus waveguide gives an evenly
  spaced comb of resonance notches ($T_a$); a pump ($\omega_p$) on one resonance
  seeds signal ($\omega_s$) and idler ($\omega_i$) photons by spontaneous
  four-wave mixing on the symmetric comb lines. Three consecutive lines are
  labelled I (idler), $k$ (the $k$-th comb line), and S (signal).
  \textbf{(b)}~Adding a tunable auxiliary ring reshapes the response: the
  main-ring resonances split ($T_a$) while the auxiliary comb ($T_b$, dashed)
  interferes with them. Controlling the auxiliary ring's coupling and detuning
  forms the interface that maps device physics to system-level
  metrics---heralded purity, brightness, and Schmidt number.}
  \label{fig:dcm-source}
\end{figure}

These device-derived quantities are precisely the parameters that determine the budget
available for coexistence with classical traffic. They define the point at which the
device layer becomes legible to the network layer above it.

\section{Distance Scaling and Quantum Repeaters}
\label{sec:qsrc-distance}

While point-to-point QKD has been demonstrated over hundreds of kilometers, it faces a fundamental scaling limit. In classical optical networks, the exponential attenuation of light in standard telecom fiber is overcome using optical amplifiers (such as erbium-doped fiber amplifiers, or EDFAs) spaced every 80 to 100 kilometers. However, the no-cloning theorem strictly forbids the deterministic amplification of unknown quantum states. 

As a result, the key rate in a direct QKD link decays exponentially with
distance. This decay is bounded by the
Pirandola--Laurenza--Ottaviani--Banchi (PLOB) secret-key
capacity~\cite{pirandola2017fundamental},
\begin{equation}
  R_\mathrm{PLOB} \le -\log_2(1-\eta), \qquad \eta = 10^{-\alpha L/10},
  \label{eq:plob}
\end{equation}
where $\eta$ is the channel transmittance for a fibre of length $L$ and
attenuation $\alpha$ (typically $0.2$~dB/km). Since $\eta$ falls exponentially
with $L$, so does $R_\mathrm{PLOB}$. While state-of-the-art techniques like
Twin-Field QKD (TF-QKD)~\cite{lucamarini2018overcoming} improves the asymptotic
scaling to $R_\mathrm{TF} \le -\log_2(1-\sqrt{\eta})$. This effectively halves the
loss exponent and pushes the maximum transmission distance past 800
kilometers~\cite{wang2022twinfield}. However, the resulting secure key rates at these
extreme distances are often on the order of bits per second, which is insufficient for
encrypting high-volume datacenter traffic. Figure~\ref{fig:qkd-distance-rate} summarizes this distance--rate tradeoff: state-of-the-art demonstrations cluster below the repeaterless bound, and only quantum repeaters restore favourable scaling at long distance.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\linewidth]{figures/chapter3/distance_keyrate.pdf}
  \caption[Distance scaling of QKD secret key rate]{Secret key rate versus transmission distance for quantum key distribution. Direct links are bounded by the PLOB limit~\cite{pirandola2017fundamental} (grey dashed) and decay exponentially. Twin-Field QKD (orange dashed) improves scaling to $\sqrt{\eta}$, extending reach past $1000$~km but at very low rates. Quantum repeaters (green dash--dot, illustrative) are required to restore polynomial scaling and achieve the shaded region above the PLOB bound. Markers indicate state-of-the-art demonstrations~\cite{wang2025highrate,dynes2016ultrahigh,dolphin2023hybrid,ribezzo2023underwater,chen2021twinfield511,pittaluga2021repeaterlike,liu2023experimental1000,zhou2023twinfield}. Bounds span $0.16$--$0.2$~dB/km fibre attenuation, assuming a $1$~GHz clock.}
  \label{fig:qkd-distance-rate}
\end{figure}

To overcome this distance-rate tradeoff and achieve polynomial scaling, a global quantum network requires \emph{quantum repeaters}~\cite{briegel1998quantum}. Quantum repeaters divide a long-distance link into shorter, manageable segments. They operate by establishing entanglement across each short segment and then using entanglement swapping (a joint Bell-state measurement) to stitch these segments together, effectively teleporting the entanglement to the endpoints. 

The realization of quantum repeaters relies heavily on two components: quantum memories to store states until adjacent links succeed, and highly efficient, scalable sources of entangled photons to continuously populate the links. This necessity places immense pressure on the design of the photon sources: they must be bright, deterministic (or highly multiplexed), and spectrally compatible with the optical fibers carrying them.

\section{Coexistence with Classical Infrastructure}
\label{sec:qsrc-coexistence}

A further challenge for deploying quantum networks is the economic unfeasibility of laying dedicated ``dark fiber'' exclusively for quantum traffic. Practical quantum networking must co-exist with classical optical communications on the same fiber infrastructure using Wavelength Division Multiplexing (WDM). 

Integrating single-photon quantum channels alongside classical channels carrying milliwatts of power is notoriously difficult. The primary obstacle is spontaneous Raman scattering: the intense classical lasers scatter photons across a broad spectrum within the fiber, creating a noise floor that easily overwhelms the fragile quantum signals.

Overcoming Raman noise and achieving coexistence requires precise spectral filtering and alignment with the International Telecommunication Union (ITU) telecom grid. The entangled photon sources must generate narrow-linewidth states at specific, standardized frequencies, and their emission characteristics must remain perfectly stable over time to allow ultra-tight filtering at the receiver. This requirement elevates the photon source from a physics experiment to a highly constrained network engineering component.

\section{Contribution and Articles}

This chapter is built around two contributions that move the dual-coupled microring (DCM) source from a promising integrated-photonics device toward a predictable network-engineering component.

The first contribution (P1) establishes the theoretical and modeling foundation for the chapter. It presents a coupled-mode-theory digital twin for the DCM source, mapping low-level physical parameters like auxiliary-cavity detuning to system-facing metrics such as photon-pair purity and brightness. The results were accepted and presented at the 26th International Conference on Transparent Optical Networks (ICTON 2026).

The second contribution (P2) provides the experimental validation of the device's stability. It demonstrates that thermal tuning of the auxiliary resonator preserves the predictable free spectral range behavior required for channelized quantum network operation. This work was accepted and presented at the 2026 International Conference on Science and Electrical Engineering (ICSEE).

Together, the two articles establish that source behavior can be modeled, optimized, and physically stabilized in terms that later network layers can consume. This addresses the device-level aspects of \textbf{RQ1}, demonstrating how integrated photonic sources can be engineered to generate predictable quantum resources. The following chapters build on this foundation by moving from generating quantum-secure optical resources to processing the resulting classical information on programmable network hardware.

\includedpaper{P1}{Simulation-Driven Optimization of Dual-Coupled Micro-Ring Entanglement Sources for Quantum Optical Networks}{meyer2026icton}{source-materials/papers/camera-ready/dcm-entanglement-sources-quantum-networks-camera-ready.pdf}

\includedpaper{P2}{Free Spectral Range Stability under Linear Thermal Tuning in a Dual-Coupled Microring Resonator}{meyer2026icsee}{source-materials/papers/camera-ready/fsr-stability-dual-coupled-microring-camera-ready.pdf}
