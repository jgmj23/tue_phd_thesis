\chapter{Observability for Confidential AI Infrastructure}
\label{ch:confidential-ai}

\section{Detecting Threats Without Payload Access}

As AI models become astronomically expensive to train, protecting the model weights and
training data from unauthorized access becomes critical. Confidential computing addresses this
by using hardware-based trusted execution environments (TEEs), such as AMD SEV-SNP or Intel TDX,
combined with strictly encrypted memory and PCIe links \textsc{TODO: citation (confidential computing/TEE overview)}.
This ensures that the tenant's workload is protected even from a privileged hypervisor or a
malicious infrastructure operator.

However, the same cryptographic boundary that shields tenant data also blinds the defender.
In a confidential AI training cluster, payloads are entirely opaque to the infrastructure provider.
Classical intrusion detection systems (IDS), which rely on deep packet inspection and payload
signature matching, are rendered useless.

The background question for this part of the thesis is therefore: what observable signal
survives the encryption and TEE boundaries? The answer lies in metadata, traffic structure,
packet timing, and hardware telemetry extracted directly from the DPU. By combining these
side-channel signals, it is possible to detect threats, such as data exfiltration or malware
beaconing, without ever accessing the payload. This reframes the DPU of Chapter~\ref{ch:background}
from merely an accelerator into a privileged vantage point, extending the thesis focus from
prevention and acceleration toward detection and response.

% TODO: Expand with paper-specific framing for P8 and P9.

\section{Contribution and Articles}

This chapter is built around two contributions that study security monitoring for confidential AI infrastructure without inspecting payloads.

The first contribution (P8), \enquote{Cross-Layer Observability for Intrusion Detection in Confidential AI Training Clusters}, develops a cross-layer observability framework for intrusion detection in confidential AI training clusters.
% TODO: Record the author's specific contributions and co-author context for P8.

The second contribution (P9), \enquote{Obscured, But Not Blind: Systematizing Observability in Confidential AI Training Clusters}, systematizes the observability that survives encryption and trusted-execution-environment boundaries, and validates it with worked-example measurements when payload access is unavailable.
% TODO: Record the author's specific contributions and co-author context for P9.

TODO: Synthesize what the papers show about metadata, telemetry, DPU observability, and security monitoring without payload inspection, and transition from the final paper-bearing chapter to the thesis-wide synthesis of cross-layer lessons.

\includedpaper{P8}{Cross-Layer Observability for Intrusion Detection in Confidential AI Training Clusters}{meyer2026intrusion}{source-materials/papers/camera-ready/cross-layer-observability-confidential-ai-training-clusters-camera-ready.pdf}

\includedpaper{P9}{Obscured, But Not Blind: Systematizing Observability in Confidential AI Training Clusters}{meyer2026detecting}{source-materials/papers/camera-ready/Conf_Compute_TIFS.pdf}
